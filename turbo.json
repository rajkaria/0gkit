{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", "build/**"],
      "env": ["NODE_ENV", "VERCEL_ENV"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "@foundryprotocol/0gkit-cli#test": {
      "dependsOn": ["^build", "@foundryprotocol/0gkit-chain#test"],
      "outputs": ["coverage/**"]
    },
    "@foundryprotocol/0gkit-mcp#test": {
      "dependsOn": ["^build", "@foundryprotocol/0gkit-chain#test"],
      "outputs": ["coverage/**"]
    },
    "clean": {
      "cache": false
    }
  }
}

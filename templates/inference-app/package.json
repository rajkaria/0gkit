{
  "name": "inference-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Run an OpenAI-compatible chat completion on a 0G Compute provider with @foundryprotocol/0gkit-compute.",
  "scripts": {
    "start": "tsx src/index.ts"
  },
  "engines": {
    "node": ">=20.10"
  },
  "dependencies": {
    "@foundryprotocol/0gkit-core": "^1.0.0",
    "@foundryprotocol/0gkit-compute": "^1.0.0",
    "@0gfoundation/0g-compute-ts-sdk": "^0.3.0",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
